using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InventoryScript : MonoBehaviour
{

    struct Item {
        // Using a simple defense/attack style stat system to test, also using an int system for item types where say 0 is a weapon, 1 is a headpiece etc.
        public Item(int type, int defense, int attack)
        {
            int itemtype = type;
            int defenseStat = defense;
            int attack = 
        }
    }


    void Start()
    {
        
    }

    
    void Update()
    {
        
    }
}
